

export type StuffItemType = {
    firstName: string,
    lastName: string,
    address: string,
    position: string,
    email: string,
    id?: string
}

export type PositionItemType = {
    positionName: string,
    salary: string,
    id: string
}